document.getElementById("reservation-form").addEventListener("submit", function (event) {
    event.preventDefault();
    alert("Réservation soumise avec succès ! Nous vous contacterons pour confirmer les détails.");
    // Vous pouvez ajouter ici le code pour envoyer la réservation à un serveur, si nécessaire.
});
